<?php
require_once __DIR__ . '/../includes/bootstrap.php'; require_once __DIR__ . '/../config/db.php';
$message=null; $error=null;
if($_SERVER['REQUEST_METHOD']==='POST'){if(!verify_csrf($_POST['csrf_token']??null))$error='Your session expired. Please try again.';elseif(!filter_var($_POST['email']??'',FILTER_VALIDATE_EMAIL))$error='Enter a valid email address.';else{$message='If an account exists for that email, password-reset instructions will be sent shortly.';/* Integrate PHPMailer here: create a random token, store its hash and expiry in a password_resets table, then email the reset link. */}}
$pageTitle='Reset password';require __DIR__.'/../includes/header.php'; ?>
<main class="auth-page"><section class="auth-card"><a class="brand d-block text-center mb-4" href="<?= base_url('index.php') ?>"><span class="brand-mark">T</span>TutorLagbe</a><h1 class="h3 text-center">Reset your password</h1><p class="text-secondary text-center mb-4">Enter your email and we’ll help you get back in.</p><?php if($message): ?><div class="alert alert-success"><?= e($message) ?></div><?php endif; ?><?php if($error): ?><div class="alert alert-danger"><?= e($error) ?></div><?php endif; ?><form method="post"><input type="hidden" name="csrf_token" value="<?= e(csrf_token()) ?>"><label class="form-label">Email address</label><input class="form-control mb-4" type="email" name="email" required autocomplete="email"><button class="btn btn-brand w-100 py-2">Send reset link</button></form><p class="text-center small mt-4 mb-0"><a class="text-success" href="login.php">← Back to login</a></p></section></main><?php require __DIR__.'/../includes/footer.php'; ?>
